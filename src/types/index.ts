export interface Contact {
  id: string;
  firstName: string;
  lastName: string;
  age: number;
}

export interface ProfileData {
  firstName: string;
  lastName: string;
  profileImage: string | null;
}
